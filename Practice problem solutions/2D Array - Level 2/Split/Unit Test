public static int[][] split(int[][] arr, int row, int col)
{
    row++;
    col++;
    
    int[][] split = new int[row][col];
    
    for (int i = 0; i < row; i++) {
        for (int j = 0; j < col; j++) {
            split[i][j] = arr[i][j];
        }
    }
    return split;
}