public int max(ArrayList<Integer> list)
{
    if (list.size() == 0) {
        return 0;
    }
    
    int max = list.get(0);
    for (int num : list) {
        if (num > max) {
            max = num;
        }
    }
    return max;
}