public static boolean pairs(int[] arr)
{
    for (int i = 0; i < arr.length; i++) {
        if (i != arr.length - 1 && arr[i] == arr[i + 1]) {
            return true;
        }
    }
    return false;
}