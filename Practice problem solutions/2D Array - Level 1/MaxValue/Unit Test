public static int max(int[][] arr)
{
    int value = -1;
    
    for (int i = 0; i < arr.length; i++) {
        for (int j = 0; j < arr[i].length; j++) {
            if (value <= arr[i][j]) {
                value = arr[i][j];
            }
        }
    }
    return value;
}